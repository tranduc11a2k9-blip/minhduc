#pragma once

// Free Fire (normal) + Free Fire MAX share a runtime-selected offset table.
// - FF values: GameOffsets.mm (kOffsetsFF)
// - Max values: offsetmax.h (edit here for Max)
// - Active table: GameOffsetsCurrent() / k* macros below

#include "GameOffsets.h"
